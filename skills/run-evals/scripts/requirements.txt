# No third-party runtime dependencies.
# Everything uses the Python 3 standard library.
#
# This file is intentionally empty so that a documented
# `pip install -r requirements.txt` invocation is still a no-op success.
